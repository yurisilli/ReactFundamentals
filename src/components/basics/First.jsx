import React from "react";

export default function Primeiro() {
    const subTitle = "Welcome";

    return (
        <div>
            <h2>First Component</h2>
            <h3>{subTitle}</h3>
        </div>
    );
}
